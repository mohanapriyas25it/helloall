name: mohana
on:
 workflow_dispatch
jobs:  
  test:
    runs-on: ubuntu-latest
    steps:
      - name: printing hello
        run: echo "Hello"

        build:
          runs-on: ubuntu-latest
          needs: test
          steps:
            - run: echo "Building project"

        deploy:
          runs-on: ubuntu-latest
          needs: build
          steps: 
            - run: echo "Deploying"